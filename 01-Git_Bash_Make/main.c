#include <stdlib.h>
#include <stdio.h>

int main()
{
        int number;

        for (int i = 0; i < 10; ++i) {
                printf("\nInsert the number -> ");
                scanf("%d", &number);
        }

        return 0;
}
