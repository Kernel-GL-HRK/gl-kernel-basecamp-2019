#! /bin/bash

TMPDIR=/tmp/guessanumber
TARNAME=guessanumber.tar.gz
FINDIR=release

if [ ! -d $TMPDIR ]; then
	mkdir $TMPDIR
fi

cp *.c $TMPDIR
cp *.h $TMPDIR

tar -zcvf $TARNAME $TMPDIR

if [ ! -d release ]; then
	mkdir release
fi

cp $TARNAME $FINDIR
